# Настройка Inpaint Editor с Telegram Mini App

Этот гайд поможет настроить функцию "Дорисовать" с интерактивным редактором маски.

## Что было добавлено

1. **Telegram Mini App** - веб-редактор для рисования маски прямо в Telegram
2. **Flask веб-сервер** - обслуживает Mini App и обрабатывает изображения
3. **Интеграция в бота** - кнопки "Дорисовать" теперь открывают редактор

## Требования

```bash
pip install flask flask-cors pillow requests
```

## Шаг 1: Настройка окружения

Добавьте в `.env` файл:

```env
# Mini App Web Server URL
WEBAPP_URL=https://your-domain.com
```

**Для разработки** (localhost):
```env
WEBAPP_URL=http://localhost:5000
```

**Для продакшена** нужен публичный HTTPS URL (можно использовать ngrok, Cloudflare Tunnel, или VPS).

## Шаг 2: Запуск веб-сервера

### Локально (для тестирования)

```bash
python webapp_server.py
```

Сервер запустится на `http://0.0.0.0:5000`

### С ngrok (для тестирования через интернет)

1. Установите ngrok: https://ngrok.com/download
2. Запустите веб-сервер:
   ```bash
   python webapp_server.py
   ```
3. В другом терминале:
   ```bash
   ngrok http 5000
   ```
4. Скопируйте HTTPS URL из ngrok (например: `https://abc123.ngrok.io`)
5. Обновите `.env`:
   ```env
   WEBAPP_URL=https://abc123.ngrok.io
   ```

### На продакшене (VPS)

1. Загрузите файлы на VPS:
   ```bash
   scp -r static/ webapp_server.py root@your-server:/path/to/bot/
   ```

2. Установите зависимости на сервере:
   ```bash
   ssh root@your-server
   cd /path/to/bot
   pip install flask flask-cors pillow requests
   ```

3. Запустите веб-сервер с gunicorn (рекомендуется):
   ```bash
   pip install gunicorn
   gunicorn -w 4 -b 0.0.0.0:5000 webapp_server:app
   ```

4. Настройте nginx как reverse proxy:
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;

       location / {
           proxy_pass http://127.0.0.1:5000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```

5. Настройте HTTPS с Let's Encrypt:
   ```bash
   certbot --nginx -d your-domain.com
   ```

6. Обновите `.env`:
   ```env
   WEBAPP_URL=https://your-domain.com
   ```

## Шаг 3: Запуск бота

После запуска веб-сервера, запустите бота как обычно:

```bash
python bot.py
```

## Как это работает

1. Пользователь нажимает кнопку "🎨 Дорисовать" после `/editmy` или после генерации
2. Бот загружает изображение на веб-сервер
3. Открывается Telegram Mini App с редактором маски
4. Пользователь рисует маску кисточкой и нажимает "Готово"
5. Маска отправляется обратно в бот
6. Бот запрашивает описание для inpaint
7. Выполняется inpainting с использованием Stability AI API

## Структура файлов

```
imagegen/
├── bot.py                          # Основной файл бота (модифицирован)
├── webapp_server.py                # Flask сервер для Mini App (новый)
├── settings.py                     # Настройки (добавлен WEBAPP_URL)
├── static/                         # Статические файлы для веб-сервера
│   ├── inpaint_editor.html        # Mini App редактор (новый)
│   └── images/                     # Директория для временных изображений
└── INPAINT_SETUP.md               # Эта инструкция

```

## Troubleshooting

### Ошибка: "Ошибка загрузки редактора"

- Убедитесь, что веб-сервер запущен
- Проверьте, что WEBAPP_URL указан правильно в .env
- Проверьте логи веб-сервера

### Кнопка открывает пустую страницу

- Убедитесь, что файл `static/inpaint_editor.html` существует
- Проверьте права доступа к файлу

### Mini App не отправляет маску

- Проверьте консоль браузера на ошибки (откройте DevTools в Telegram)
- Убедитесь, что используется HTTPS (для продакшена)

### Изображение не загружается в редакторе

- Проверьте CORS настройки в webapp_server.py
- Убедитесь, что директория `static/images/` существует и доступна для записи

## Безопасность

⚠️ **Важно для продакшена:**

1. Используйте HTTPS (обязательно для Telegram Mini Apps)
2. Добавьте аутентификацию для загрузки изображений
3. Ограничьте размер загружаемых файлов
4. Регулярно очищайте директорию `static/images/` от старых файлов
5. Используйте валидацию Telegram WebApp initData

## Дополнительные возможности

### Добавление ластика

В `inpaint_editor.html` можно добавить кнопку для стирания маски (рисование черным цветом).

### Сохранение истории масок

Добавьте больше слотов в историю для множественных undo/redo операций.

### Предпросмотр маски

Добавьте переключатель для показа/скрытия маски поверх изображения.

## Поддержка

Если возникли проблемы, проверьте:
- Логи бота: `bot.py` выводит в консоль
- Логи веб-сервера: `webapp_server.py` выводит в консоль
- Telegram Bot API логи
