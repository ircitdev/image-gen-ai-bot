# Быстрый старт Inpaint Editor

## ⚠️ ВАЖНО: Текущая ситуация

Вы получили ошибку "кнопка зависает" потому что **веб-сервер не запущен**.

## ✅ Решение - 2 простых шага:

### Шаг 1: Запустите веб-сервер

Откройте **новое** окно терминала и выполните:

```bash
python webapp_server.py
```

**ИЛИ** (на Windows) просто дважды кликните:
```
start_webapp.bat
```

Вы должны увидеть:
```
Starting Mini App server on http://0.0.0.0:5000
 * Running on http://0.0.0.0:5000
```

### Шаг 2: Перезапустите бота

В другом окне терминала:
```bash
python bot.py
```

## 🎨 Теперь всё работает!

1. Отправьте `/editmy` боту
2. Загрузите фото
3. Нажмите **"🎨 Дорисовать"**
4. Откроется интерактивный редактор!

---

## 📌 Альтернатива (если не хотите запускать веб-сервер)

Бот теперь показывает инструкцию как использовать inpaint **БЕЗ** веб-сервера:
- Создайте маску вручную в Photoshop/GIMP
- Белые области = что изменить
- Черные области = оставить как есть
- Отправьте маску боту

## 🔧 Troubleshooting

### "Cannot connect to webapp server"
→ Веб-сервер не запущен. Запустите `python webapp_server.py`

### "Address already in use"
→ Порт 5000 занят. Измените в `webapp_server.py`:
```python
app.run(host='0.0.0.0', port=5001)  # Измените на 5001
```

И в `.env`:
```
WEBAPP_URL=http://localhost:5001
```

### Бот всё равно не открывает редактор
→ Проверьте `.env` файл:
```env
WEBAPP_URL=http://localhost:5000
```

## 📚 Полная документация

См. `INPAINT_SETUP.md` для:
- Настройки на VPS
- Использования с ngrok
- HTTPS конфигурации
- Продакшен деплоя
